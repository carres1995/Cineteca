// src/test/jest-dom.d.ts
// Los matchers de jest-dom se declaran acá para que TypeScript y el linter los
// vean dentro de `src`: sin esto, `toBeInTheDocument()` es una llamada sin tipo.
import '@testing-library/jest-dom/vitest';
